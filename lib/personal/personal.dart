import 'package:flutter/cupertino.dart';

/*
Personal Page:
Currently a template page showing basic text
In the future, it will display settings and personal info (if permitted by St. X staff), such as grades
 */

class Personal extends StatelessWidget {
  const Personal({super.key});

  @override
  Widget build(BuildContext context){
    return const Text('Chat Page');
  }
}