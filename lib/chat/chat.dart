import 'package:flutter/cupertino.dart';

/*
Chat Page:
Currently a template page showing basic text
In the future, this will be the forums page that computer science club has been trying to make
 */

class Chat extends StatelessWidget {
  const Chat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Chat Page');
  }
}
